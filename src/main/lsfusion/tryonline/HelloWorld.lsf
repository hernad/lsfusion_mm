// # header
MODULE HelloWorld;
// # header end
run () {
     exportString () <- 'Hello world!'; // export * properties are the result of the default HTTP request
    
     // alternative way
     // EXPORT FROM 'Hello world!'; // exports to JSON by default and writes the result as JSONFILE to exportFile
}
